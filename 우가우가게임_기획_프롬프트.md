# "우가우가게임" 구현 프롬프트 (Claude Code에 붙여넣을 것)

## 배경

기존 `leftover` 레포에는 `vision-server/`(Python, MediaPipe Pose Landmarker로 웹캠에서 상체
관절을 인식해 UDP로 이벤트를 보내는 서버)와 `pc-game/`(Unity, 3D 검투 게임 프로토타입)이
있다. **3D 검투 게임 컨셉은 전부 폐기한다.** `vision-server/`의 "웹캠 → MediaPipe → UDP로
Unity에 전송" 기본 골격(포트 구조, JSON 직렬화 패턴, 백그라운드 스레드+큐로 Unity 메인
스레드에 안전하게 전달하는 패턴)은 검증된 코드이니 재사용하되, 인식 대상과 전송 내용, 그리고
Unity 쪽 프로젝트는 완전히 새로 만든다: **Unity 2D**, 캐릭터는 원시인, 미니게임 6종을 순서대로
진행하는 1:1 파티 게임.

## 전체 컨셉

두 사람이 각자 자기 카메라(노트북 웹캠 등) 앞에 선다. 처음에는 웹캠 한 대 앞에 두 사람이
같이 서서 화면 좌/우로 구분하는 구조로 설계했지만, 실제로 만들어서 테스트해보니 두 사람이
붙어 있으면 사람 감지 단계에서 한쪽이 인식되지 않는 문제가 확인돼 **플레이어 1명당 카메라
1대(온라인 모드)** 로 바꿨다 — 각자의 카메라는 자기 자신만 인식하면 되므로 이 문제 자체가
사라진다. Unity는 두 PC 중 한쪽(또는 별도 PC)에서만 실행되고, 두 사람은 그 화면을 같이
보면서 플레이한다(화면이 두 개로 나뉘어 각자 다른 화면을 보는 구조는 아님 — 진짜 원격/분산
멀티플레이는 이번 범위 밖). 각자 원시인 캐릭터로 게임에 등장하며, 6개의 미니게임을 **고정된
순서로 한 판씩** 진행한다. 각 미니게임은 승/패가 명확히 갈리고, 6판이 끝나면 더 많이 이긴
사람이 최종 승자.

**캐릭터 아트**: 별도 스프라이트 리소스 없이 **심플한 도형/실루엣**으로 원시인을 표현한다(원형
머리, 캡슐형 몸통, 선 굵기 있는 팔다리 등 — Rive/Papercut 스타일이 아니라 그냥 코드로 그리는
도형 콤보 수준으로 충분). 플레이어 1/2는 색상(예: 파랑/빨강)으로만 구분.

## 기술 아키텍처

```
[카메라 A, P1의 PC] → vision-server(Python, MediaPipe, --player-id p1)  ─┐
[카메라 B, P2의 PC] → vision-server(Python, MediaPipe, --player-id p2)  ─┤ UDP 9100
                                                                          │ (같은 LAN)
                                                                          v
                                                     pc-game(Unity 2D) - 한 PC에서만 실행,
                                                     두 사람이 이 화면을 같이 봄
```

각 vision-server 인스턴스는:
- Pose Landmarker: 상체+얼굴 위치 기반 관절(어깨/팔꿈치/손목/코/엉덩이/무릎/발목) — 자기
  카메라에 잡힌 사람을 실행 인자로 받은 고정 id(`p1`/`p2`)로 라벨링(카메라 1대=1명이라 좌/우
  구분 자체가 필요 없음)
- Face Landmarker: 눈 랜드마크(EAR 계산용) — Pose와 별도 모델이지만 같은 루프에서 같은
  프레임에 대해 함께 추론
- UDP로 Unity PC에 프레임마다 연속 전송(기존처럼 "인식된 동작 이벤트" 이산 전송이 아니라
  관절 좌표/눈 상태를 그대로, 매 프레임 스트리밍 — 미니게임들이 필요로 하는 값이 제각각이라
  분류를 미리 서버에서 끝내기보다 원시값을 보내고 Unity 쪽에서 게임별로 해석하는 게 더 유연함)

두 인스턴스가 각자 자기 담당 플레이어 한 명만 담긴 프레임을 같은 포트로 보내고, Unity의
`PoseInputHub`는 프레임에 들어온 id만 갱신하는 구조라 두 스트림이 자연스럽게 합쳐진다 —
카메라 1대에 두 명을 같이 인식하던 초기 방식(구모드)은 `vision-server/README.md`에 남아있다.

### 공용 모듈 (모든 미니게임이 재사용)

- **P1/P2 랜드마크 스트림 수신기**: UDP 수신 → 최신 프레임의 P1/P2 각각의 관절 좌표를 보관하는
  단일 컴포넌트. 모든 미니게임 씬이 이걸 통해서만 입력을 읽는다(미니게임 로직이 UDP/JSON을
  직접 몰라도 되게).
- **손 들기 인식**: 손목 y좌표가 어깨 y좌표보다 일정 값 이상 위에 있으면 "들었다"로 판정, 왼손/
  오른손 각각 독립 판정.
- **머리 기울기 인식**: 코-어깨중심 각도 또는 귀 좌표 기울기로 좌/우 기울임 판정.
- **입 벌림 인식**: 입술 랜드마크 상하 간격이 기준치 이상 벌어지면 "벌림"으로 판정.
- **점프 높이 계산**: 각 사람마다 게임 시작 시 1~2초간 "가만히 서 있는" 발 위치를 기준선으로
  캘리브레이션하고, 이후 발(또는 엉덩이 중심) y좌표가 그 기준선에서 얼마나 올라갔는지로 높이
  측정 — 실제 키/캠 거리 차이에 영향 안 받게.
- **눈 감김 인식(EAR)**: Face Landmarker의 눈 랜드마크로 Eye Aspect Ratio 계산, 임계값 이하가
  일정 시간(기본 0.4초) 이상 지속되면 "눈 감음"으로 판정 — 순간적인 자연 깜빡임은 무시.
- **손-머리 거리 인식**: 코코넛 깨기용, 양손 중심과 머리 중심 사이 거리를 매 프레임 계산.

## 미니게임별 상세 스펙

### 1. 돌던지기

- **조작**: 오른손을 들면 오른손 발사 위치, 왼손을 들면 왼손 발사 위치가 활성화. 머리를
  좌/우로 기울이면 그 방향으로 회피 자세.
- **핵심 메커니즘 (기관총식 연속 발사)**: 어느 쪽이든 손이 들려 있는 동안, **약 1초마다 자동으로
  돌 하나가 발사**된다. 플레이어는 발사되는 순간의 "어느 손이 들려 있는지"로 돌이 날아가는
  경로(좌/우 중 하나, 상대 캐릭터의 왼쪽/오른쪽 중 하나를 조준)를 계속 바꿀 수 있다. 즉 단발
  트리거가 아니라 **지속적으로 손을 바꿔가며 조준을 흔드는 실시간 공방**.
- **피격 판정**: 발사된 돌의 목표 방향(상대의 왼쪽 or 오른쪽)과, 그 발사 순간 상대가 기울이고
  있는 머리 방향이 다르면 명중, 같으면 회피.
- **승리 조건**: 정해진 시간(기본 30초) 안에 상대를 더 많이 맞힌 사람 승리.
- **조정 가능 값**: 발사 주기(기본 1초), 제한시간(기본 30초).

### 2. 자세 따라하기 (인간 테트리스)

- **턴 구조**: "모션 정하기 턴"과 "플레이 턴"이 번갈아 진행.
  - 모션 정하기 턴: 자신의 차례인 사람이 일정 시간(기본 2초) 동안 자세를 유지하면 그 포즈가
    캡처되어, **상대방 쪽 트랙에 그 실루엣 모양의 구멍이 있는 벽**으로 생성된다.
  - 플레이 턴: 자신에게 다가오는 벽(상대가 만든 포즈 구멍)에 맞춰 그 포즈를 취해야 한다.
- **판정**: 벽이 도달하는 순간 자신의 관절 좌표가 구멍 실루엣과 일정 허용 오차(기본 관절별
  거리 오차 15% 이내) 안에 들어오면 통과, 아니면 벽에 닿은 것으로 처리.
- **시각화**: **2D 캐릭터가 좌우로 긴 트랙 위에 서 있고**, 벽에 닿을 때마다 트랙을 따라 일정
  거리(기본 트랙 길이의 10%) 뒤로 밀려나는 것을 화면에 직접 보여준다 (Fit-in/사족보행 게임처럼
  트랙이 시각적으로 존재).
- **승리 조건**: 정해진 라운드 수(기본 왕복 3회, 즉 각자 3번씩 포즈 지정) 이후, 트랙에서 덜
  밀려난(원점에 더 가까운) 사람이 승리.
- **조정 가능 값**: 포즈 유지 시간(2초), 허용 오차(15%), 라운드 수(3), 밀려나는 거리(10%).

### 3. 점프해서 과일따기

- **트랙 구조**: 화면 좌/우에 **각자의 나무가 독립적으로 존재** (상대와 자원 경쟁 없음).
- **점프 높이 계산**: 공용 모듈의 캘리브레이션된 기준선 대비 상승 높이로 판정 (키 차이 영향
  없음).
- **과일 단계**: 3단계 — 낮은 단(기본 1점, 자주 딸 수 있음: 판정 쿨다운 짧음), 중간 단(3점),
  높은 단(5점, 높이 점프 필요, 쿨다운 김). 정확한 높이 임계값/쿨다운은 구현 중 실측 후 조정.
- **먹기 판정**: 해당 높이에 도달한 시점(또는 착지 직후 짧은 윈도우 내)에 **입을 벌리고
  있어야만** 과일 획득 인정.
- **승리 조건**: 제한시간(기본 30초) 동안 더 많은 점수를 딴 사람 승리.
- **조정 가능 값**: 제한시간(30초), 각 단 높이 임계값/점수/쿨다운.

### 4. 머리로 코코넛 깨기 (반복 속도 경쟁)

- **조작**: 양손을 모아 코코넛을 든 자세를 취한 뒤, 손을 머리 쪽으로 가져갔다가(내리침) 다시
  떨어뜨리는 동작을 반복한다.
- **판정**: 손-머리 거리가 일정 값 이하로 좁혀졌다가(타격) 다시 일정 값 이상 벌어지는(회수)
  한 사이클을 "1회 타격"으로 카운트.
- **승리 조건**: 정해진 시간(기본 15초) 동안 **더 많이 타격 사이클을 완료한 사람이 승리** —
  순수 반복 속도 경쟁, 내구도/단계 개념 없음.
- **조정 가능 값**: 제한시간(15초), 타격/회수 판정 거리 임계값.

### 5. 돌 or 바나나

- **역할**: 매 턴 던지는 사람과 받는 사람 역할이 번갈아 바뀐다 (1턴: P1이 던짐/P2가 받음, 2턴:
  P2가 던짐/P1이 받음, ...).
- **선택 동작**: 던지는 사람은 **손 종류로 선택** — 오른손을 들면 돌, 왼손을 들면 바나나를
  던진다 (돌던지기 미니게임과 동일한 제스처 문법이라 학습 부담이 적음). 실제로 무엇을 던졌는지는
  **화면에 물리적으로 보임** (숨김 없음 — 상대가 보고 반응하는 리액션 게임).
- **받는 사람 동작**: **입을 벌리면 "먹기"**, 입을 다물고 있거나 고개를 돌리면 "피하기".
- **결과**:
  - 바나나를 던졌는데 안 먹음(피함) → 바나나가 부메랑처럼 돌아와 **던진 사람이 먹어야 함**
    (다시 같은 판정 — 이번엔 던진 사람이 입을 벌리고 있어야 포만감 획득, 못 벌리면 그냥 놓침).
  - 바나나를 먹음 → 받는 사람 포만감 게이지 증가.
  - 돌을 던졌는데 먹어버림(실수로 입 벌림) → **순수 페널티**: 포만감 증가 없이 이빨만 소모.
  - 돌을 피함 → 페널티 없음, 아무 일도 없음.
- **이빨/패배 조건**: 돌을 먹을 때마다 이빨 게이지가 줄어들고, **이빨이 0이 되면 그 즉시 패배**
  (포만감을 다 채우기 전이라도). 정상적으로는 **포만감 게이지를 먼저 채운 사람이 승리**.
- **조정 가능 값**: 포만감/이빨 게이지 최대치, 턴당 제한시간.

### 6. 눈빛 싸움

- **조작 없음** — 그냥 카메라를 계속 응시.
- **판정**: Face Landmarker의 눈 랜드마크로 EAR(Eye Aspect Ratio)을 매 프레임 계산, 임계값
  이하가 **0.4초 이상 연속** 지속되면 "눈 감음"으로 확정 — 짧은 생리적 깜빡임은 무시.
- **승리 조건**: 먼저 "눈 감음"이 확정된 사람이 즉시 패배. 무승부 방지를 위해 최대 제한시간
  (기본 60초) 도달 시 무승부 처리(또는 재시작) 규칙 추가 권장.
- **조정 가능 값**: EAR 임계값(캘리브레이션 필요할 수 있음 — 사람마다 눈 모양이 달라 시작 시
  "눈을 크게 뜬 상태"를 잠깐 캘리브레이션하는 걸 권장), 눈 감음 판정 지속시간(0.4초), 최대
  제한시간(60초).

## 구현 우선순위 제안

1. 공용 모듈부터: P1/P2 좌우 분리 + UDP 스트리밍 파이프라인(vision-server 쪽 최소 수정 +
   Unity 수신기) → 이게 되어야 나머지 전부가 움직임.
2. 가장 단순한 것부터 프로토타입: **눈빛 싸움**(조작 없음, 판정 로직만) → **코코넛 깨기**(반복
   카운트만) → 나머지 4개.
3. 미니게임 간 공통 UI(라운드 전환 화면, 스코어보드, 승자 표시)는 마지막에 한 번에 통일.

## 아직 남은 세부 조정값들

위 "조정 가능한 값"들은 전부 실제 플레이 테스트를 거쳐야 확정되는 숫자다 — 첫 구현에서는
표시된 기본값으로 시작하고, 실측 후 체감에 맞게 조정할 것. 특히 EAR 임계값과 점프 높이 단계
구간은 사람마다/캠 위치마다 편차가 클 수 있어 캘리브레이션 단계를 넣는 걸 권장한다.

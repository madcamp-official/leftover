# 프로젝트 기획: 웹캠 모션 인식 보스전 게임 (가제)

## 1. 한 줄 컨셉

**노트북 웹캠 하나(MediaPipe Pose)로 검 스윙, 방패 방어/패링, 회피, 발차기까지
7가지 동작을 전부 인식해서 내 몸동작을 그대로 게임 캐릭터에 매핑하고, 보스와 검술
대결을 벌이는 3D 액션 게임.**

플레이어는 웹캠 앞 고정된 위치에서 상체/하체 동작만으로 대결한다. 오른손은 검, 왼손은
방패 역할을 맨몸으로 대신하고, 웹캠 하나가 이 모든 동작(검·방패·회피·발차기)을 하나의
MediaPipe Pose 파이프라인으로 인식한다.

> **폰 2대(검/방패 IMU) 방식은 폐기했다.** 초기엔 폰 센서가 스윙/패링을 더
> 정확히 잡아줄 거라 기대했지만, 실측해보니 웹캠 단독 인식 대비 딱히 정확도가 안 나오는
> 반면 페어링·시간 동기화·듀얼 네트워크 같은 복잡도만 늘었다. 웹캠(MediaPipe)만으로
> 최종 구현하는 쪽으로 확정. `prototype/pc_server/sword_shield_classifier.py`(폰 IMU
> 실험)는 이 판단의 근거로 남겨둔 기록이지 앞으로 쓸 코드는 아니다.

---

## 2. 핵심 게임 플레이

**플레이어 vs 보스(컴퓨터) 1대1 검술 대결.**

- 보스는 정해진 랜덤 패턴으로 공격 예고 → 플레이어가 타이밍 맞춰 대응
- 공격/방어는 각각 별도 게이지를 소모하는 자원 관리형 판정 시스템
- 데미지를 충분히 넣으면 클리어, 실패하면 데미지를 입음
- 최종 결과: 클리어 타임/판정 성공률 기반 랭크(S/A/B) 산정

### 동작 목록 (7종) — 전부 웹캠 MediaPipe Pose 인식

**공격 — 검(오른손으로 근사)**

| 동작 | 인식 원리 | 특징 |
|---|---|---|
| 가로 베기 | 오른쪽 손목이 몸통 중심 기준 상대적으로 좌우로 크게, 빠르게 움직임 | 표준 데미지. **앉기**로 회피 가능 |
| 세로 베기 | 오른쪽 손목이 몸통 중심 기준 상대적으로 상하로 크게, 빠르게 움직임 | 표준 데미지. **좌우 움직이기**로 회피 가능 |

**방어 — 방패(왼손으로 근사), 방어 게이지 소모. 게이지 부족 시 방어 불가**

| 동작 | 인식 원리 | 특징 |
|---|---|---|
| 기본 방어 | 왼쪽 손목이 가슴 높이 근처에서 일정 시간 이상 정지 | 방어 게이지 1.0 소모. 가로/세로베기를 막지만 발차기에는 깨져 0.25 데미지와 경직을 받음 |
| 패링 | 왼쪽 손목이 몸통 중심 기준 크게 뻗어나가는 동작 (방향 무관) | 방어 게이지 0.5 소모. 타이밍을 맞추면 모든 공격을 막고 상대를 경직시킴 |

**회피 — 전신 Pose, 게이지 소모 없음**

| 동작 | 인식 원리 | 특징 |
|---|---|---|
| 앉기 | 어깨-엉덩이 중간 y가 기준 자세보다 일정 비율 이상 내려감 | 가로베기 회피 및 상대 경직. 세로베기와 발차기에는 피격 |
| 좌우로 움직이기 | 몸통 중심 x가 기준 위치에서 일정 비율 이상 벗어남 (발은 고정) | 세로베기 회피 및 상대 경직. 발차기도 회피하며 가로베기에는 피격 |

**발차기 — 하반신 Pose**

| 동작 | 인식 원리 | 특징 |
|---|---|---|
| 발차기 | 무릎 각도(엉덩이-무릎-발목)가 짧은 시간에 급격히 펴짐. 왼발/오른발 구분 없이 `kick` 하나로만 인식 | 데미지는 기본 검 공격(가로/세로 베기)의 **절반**. **기본 방어**로 막으면 방어가 실패하면서 경직과 0.25 데미지를 받음. 발차기 도중 상대가 **가로 베기 / 세로 베기**를 하면 발차기가 캔슬되고 오히려 발차기한 쪽이 데미지 1.0을 입음. **좌우 움직이기**로는 회피 가능(경직 보너스는 없음), **앉기**로는 회피 불가, **패링**엔 완전히 막힘 |

모든 동작을 웹캠 Pose 랜드마크(손목/팔꿈치/무릎/어깨/엉덩이)만으로 근사 인식한다.
폰 IMU보다 정확도는 떨어질 수 있지만, 기기 페어링·시간 동기화·네트워크 지연 같은
복잡도가 전부 사라지고 프로세스 하나(webcam → vision-server → Unity)로 끝난다는
트레이드오프를 선택한 것.

### 밸런스 설계 원칙

세 가지 대응 수단(방어 / 패링 / 회피)이 "안전함 vs 자원 소모 vs 리스크&보상"으로 뚜렷하게 갈리도록 설계한다.

- **기본 방어**: 게이지 1.0 소모, 두 베기를 막지만 발차기에 깨짐
- **패링**: 게이지 소모 적음, 타이밍 성공 시 상대 큰 경직(하이 리스크 하이 리턴), 실패 시 그대로 피격
- **회피 (앉기/좌우)**: 게이지 소모 없음(무료), 공격 종류를 정확히 읽어야만 성공, **성공 시 상대에게 짧은 경직(반격 찬스)을 부여** — 패링보다는 약하지만 무자원으로 반격 기회를 얻을 수 있음
- **발차기**: 검 공격의 절반(0.5) 데미지를 주는 별도 공격 수단. 기본 방어로는 못 끊고
  오히려 방어 쪽이 경직당하며 데미지도 그대로 들어가고(0.25), 상대의 가로/세로 베기
  스윙에는 그대로 카운터당해 발차기한 쪽이 역으로 데미지 1.0을 입음 — 방어로는 못 끊고
  스윙 타이밍으로만 끊을 수 있어 발차기 대 스윙의 수 싸움을 새로 만든다.

방어만 있으면 "게이지 있으면 무조건 방어"로 수렴해 수 싸움이 사라지기 때문에, 회피에 반격 보상을 붙여 세 옵션이 각자 다른 이유로 선택되게 만든 것이 이번 설계의 핵심 튜닝 포인트.

### 전체 상성표 (동작 7종 × 7종, 발차기 포함)

행 = 내가 하는 행동, 열 = 상대가 하는 행동. 회피/방어 계열끼리는 동시에 낼 수 있는 조합이
아니라 해당 없음(–)으로 비워둠. 가로/세로 베기·발차기끼리 동시에 부딪히면 서로 데미지를
주고받는 "트레이드"가 된다 (보스도 같은 동작을 쓴다는 전제 — 보스 패턴이 이 셋과
안 겹치면 이 칸들은 실제로는 잘 안 나올 수 있음, 열린 질문 참고).

| 나 \\ 상대 | 앉기 | 좌우이동 | 기본방어 | 패링 | 가로베기 | 세로베기 | 발차기 |
|---|---|---|---|---|---|---|---|
| **앉기** | – | – | – | – | 회피 성공, 상대 경직 | 회피 실패, 피격 1.0 | 회피 실패, 피격 0.5 |
| **좌우이동** | – | – | – | – | 회피 실패, 피격 1.0 | 회피 성공, 상대 경직 | 회피 성공, 무피해 |
| **기본방어**(게이지1.0) | – | – | – | – | 방어 성공, 무피해 | 방어 성공, 무피해 | 방어 실패+경직, 피격 0.25 |
| **패링**(게이지0.5) | – | – | – | – | 방어 성공, 상대 경직 | 방어 성공, 상대 경직 | 방어 성공, 상대 경직 |
| **가로베기** | 회피당함, 내가 경직 | 명중, 피해 1.0 | 막힘, 무피해 | 막힘, 내가 경직 | 트레이드, 서로 1.0 | 트레이드, 서로 1.0 | 명중, 상대 캔슬+피격1.0 |
| **세로베기** | 명중, 피해 1.0 | 회피당함, 내가 경직 | 막힘, 무피해 | 막힘, 내가 경직 | 트레이드, 서로 1.0 | 트레이드, 서로 1.0 | 명중, 상대 캔슬+피격1.0 |
| **발차기** | 명중, 피해 0.5 | 회피당함, 무피해 | 관통, 상대경직+0.25 | 막힘, 내가 경직 | 캔슬됨, 피격 1.0 | 캔슬됨, 피격 1.0 | 트레이드, 서로 0.5 |

**패링이 이 표 안에서는 약점이 없다는 점을 주의**: 7가지 상대 행동 전부에 대해 "방어 성공
+ 상대 경직"으로 나오고, 게이지도 기본 방어의 절반(0.5)만 쓴다. 이 표는 "정확히 인식/타이밍이
맞았을 때"만 보여주는 것이라, 패링의 실제 리스크는 **표 밖에 있는 인식 임계값/타이밍 윈도우**
쪽에서 만들어야 한다 — 3장 참고.

### 열린 질문 (구현하면서 플레이테스트로 확정할 것)

- [x] 방어 게이지 최대치 3.0, 기본방어 1.0·패링 0.5 소모, 초당 0.38 회복
- [ ] 회피 성공 시 부여하는 경직 시간 (패링 경직보다는 짧게 — 구체적 수치는 실측)
- [ ] 판정 윈도우 값 (기획서 추천 ±250~300ms을 시작점으로, 실측 후 조정)
- [ ] 발차기 게이지 소모 여부 (검 공격과 같은 게이지를 쓰는지, 별도 자원인지, 무료인지) —
      발차기 데미지가 베기의 절반으로 확정된 만큼, 굳이 별도 게이지 없이 무료로 가도 검
      공격을 대체할 정도는 아닐 수 있음 (전체 상성표 참고)
- [ ] 보스도 가로베기/세로베기/발차기를 그대로 쓰는지 — 상성표의 "트레이드" 칸들은 보스
      패턴이 이 3개와 겹칠 때만 의미가 있음
- [ ] **패링 인식 임계값 상향 검토 중**: 상성표상 패링은 게이지도 적게 쓰면서 7가지 상대
      행동 전부를 막고 상대에게 경직까지 주는, 사실상 기본 방어의 상위호환 — 유일한 견제
      수단이 "제대로 인식/타이밍 맞추기 어려움"이므로, 모션 인식 임계값을 낮게 잡으면 밸런스가
      무너진다. 임계값을 얼마나 올려야 기본 방어와 실사용 난이도 격차가 생기는지 실측 필요
---

## 3. 기술적 도전 과제 (발표 핵심 포인트)

폰을 포기하고 웹캠 하나로 가면서 도전과제도 바뀌었다. 이제 핵심은
**"단일 웹캠 Pose 스트림에서 7가지 동작을 낮은 오탐률로, 빠르게 분류하는 것"**.

### 3-1. 동작 인식 정확도

실측하면서 실제로 부딪힌 문제들:

- **좌우 미러 반전**: `cv2.flip`으로 거울 모드를 쓰면 MediaPipe가 리턴하는 "왼쪽/오른쪽"
  랜드마크 라벨이 실제 사용자의 좌/우와 반대로 나왔다. 코드에서 한 곳(`PHYS_*` 상수)만
  뒤집어서 보정.
- **몸통 길이(분모) 노이즈**: 상대좌표 계산에 쓰는 어깨-엉덩이 거리를 프레임마다 새로
  재면 landmark 노이즈가 그대로 실려서 가만히 있어도 판정이 흔들렸다 → EMA 스무딩으로 완화.
- **가로/세로 스윙 혼동**: 손목 이동의 수평·수직 우세 축이 불명확하면 서로 겹쳐 잡힐 수
  있어 몸통 기준 상대좌표와 축별 이동량을 함께 사용한다.
- **좌우 이동 중 스윙 오탐**: 손목의 화면 절대좌표를 쓰면 몸 전체가 옆으로 움직일 때
  팔도 같이 끌려가면서 스윙으로 오판정됐다 → 몸통 중심 기준 **상대좌표**로 바꿔서 해결.
- **캘리브레이션 없이도 쓸만해야 함**: 발차기/패링처럼 고정 임계값만으로 판정하는
  동작은 사람 체형·카메라 거리에 따라 민감도가 달라질 수 있음 — 실측 캘리브레이션
  모드를 도입했다가 오히려 잘 안 맞아서 걷어낸 사례도 있다(패링).

### 3-2. 체감 반응속도

- 웹캠 처리(캡처 → MediaPipe 추론 → 판정)가 한 프로세스 안에서 끝나므로, 폰 2대 방식
  대비 네트워크 왕복 지연 자체는 사라졌다. 남은 지연은 웹캠 프레임레이트와 MediaPipe
  추론 시간뿐.
- 판정 윈도우는 관대하게(±250~300ms) 잡아 프레임 드랍/처리 지연을 흡수한다.
- 순간 이벤트(스윙/패링/발차기)는 쿨다운을 둬서 한 동작이 여러 프레임에 걸쳐
  중복 판정되는 것을 막는다.

### 3-3. 모션 분류 알고리즘 (난이도별 선택지)

| 단계 | 방법 | 난이도 |
|---|---|---|
| Tier 1 | 랜드마크 좌표/각도 임계값 기반 분류 | 낮음 (현재 이 단계, 대부분 동작 검증 완료) |
| Tier 2 | DTW(Dynamic Time Warping)로 궤적 유사도 비교 | 중 (시간 남으면 업그레이드) |
| Tier 3 | 경량 CNN/LSTM 직접 학습 | 높음 (야심작, 데이터 수집 부담) |

**현재 Tier 1로 앉기/좌우이동/가로베기/세로베기/기본방어/패링/발차기 7개 동작을 검증했다.**
플레이어마다 동작 습관이 달라 오분류가
체감 UX를 크게 해칠 수 있으므로, 필요하면 접속 시 샘플 동작 몇 회로 임계값을 보정하는
캘리브레이션 모드를 다시 검토한다 (패링에서 한 번 시도했다가 걷어낸 적 있음 — 3-1 참고).

### 3-4. 조명/카메라 환경 민감도

MediaPipe Pose 인식률은 조명과 카메라 각도에 따라 크게 달라진다. 발표 전 실제 사용할
조명 환경에서 반드시 리허설하고, `pose_confidence` 값을 미리 확인해둘 것.
플레이어마다 스윙과 발차기 습관이 달라 오분류가 체감 UX를 크게 해칠 수 있으므로,
필요하면 접속 시 샘플 동작 몇 회로 임계값을 보정하는 캘리브레이션을 추가한다.

---

## 4. 시스템 아키텍처

```
[웹캠] → vision-server (Python, MediaPipe Pose)
   → 7동작 분류 (Tier 1 임계값 기반)
   → UDP 9002 (localhost) → Unity NetworkInputProvider
   → CombatInputHub
   → 게임 로직 (게이지 소모/회복 + 데미지·경직 처리)
   → 3D 캐릭터 애니메이션 매핑 + 보스 AI
```

`vision-server/`가 분류까지 전부 끝내고 **인식된 동작 이벤트만** Unity로 보낸다 (원시
좌표를 Unity로 넘기고 Unity가 분류하는 구조가 아님). Unity 게임 로직은 `CombatInputHub`
하나만 보고 동작하도록 짜여 있어서, 입력이 `KeyboardInputProvider`(키보드 임시 입력)에서
오는지 `NetworkInputProvider`(MediaPipe)에서 오는지 몰라도 된다 — 개발 초기엔 키보드로
게임 로직만 먼저 완성하고, 나중에 Provider만 교체하면 된다.

통신 프로토콜(패킷 포맷, 포트 등)의 상세 스펙은 [`shared/PROTOCOL.md`](shared/PROTOCOL.md) 참고.

---

## 5. 일정 (8일 기준)

| Day | 내용 |
|---|---|
| 1 | **[게임]** Unity 이벤트 계약(`CombatInputHub`) + 키보드 임시 입력으로 골격 구현 · **[인식]** MediaPipe Pose로 손목/팔꿈치/무릎 랜드마크 추출 프로토타입 (병렬 진행) |
| 2 | **[게임]** 게이지·판정·데미지·회피 반격찬스 로직 완성, 키보드로 플레이 가능한 상태 · **[인식]** 7동작 임계값 분류 완성 (발차기와 앉기 구분 특히 검증) |
| 3 | `NetworkInputProvider`로 MediaPipe 이벤트 연결, 통합 테스트 및 밸런스 1차 튜닝 |
| 4 | 3D 캐릭터 애니메이션 매핑, 보스 상태머신·패턴 설계 |
| 5 | 보스 패턴 2~3개 완성, 랭크 산정, 밸런스 2차 플레이테스트 |
| 6 | 인식 정확도 튜닝 스프린트 (오탐 줄이기, 발차기·앉기 구분과 패링 임계값 조정) |
| 7 | 레이턴시 튜닝, 이펙트/사운드/UI 폴리싱 |
| 8 | 버그픽스, 백업 데모 영상 녹화, 발표 리허설 |

---

## 6. 역할 분담 제안

- **게임 (Unity)**: `CombatInputHub` 기반 게임 로직 — 게이지, 판정, 데미지, 3D 캐릭터/보스 AI, UI. `KeyboardInputProvider`로 입력 소스 없이도 독립 개발·테스트 가능
- **인식 (MediaPipe)**: `vision-server/`에서 7동작 분류 후 `shared/PROTOCOL.md` 이벤트 포맷으로 UDP 전송

두 역할의 유일한 접점은 `shared/PROTOCOL.md`의 이벤트 계약이라, 이 문서만 먼저 합의하면
이후 각자 독립적으로 작업 가능하다.

---

## 7. 리스크 및 대비책

| 리스크 | 대비책 |
|---|---|
| 조명 환경에 따른 웹캠 인식률 저하 | 발표 전 현장 조명 리허설 필수 |
| 가로/세로베기·발차기 오분류로 엉뚱한 회피 유발 | 접속 시 샘플 동작 캘리브레이션 모드 (3-4 참고) |
| 방어가 항상 안전해 회피가 무의미해지는 밸런스 붕괴 | 회피 성공 시 반격 찬스 부여로 트레이드오프 설계 (2장 참고) |
| 패링이 기본 방어의 완전 상위호환이 되는 밸런스 붕괴 | 패링 인식 임계값을 기본 방어보다 명확히 높게 잡아 실사용 난이도 격차 유지 (2장 참고) |
| 라이브 데모 중 인식 실패 | 백업 플레이 영상 사전 녹화 |
| 좌우 미러 반전 등 카메라별 랜드마크 편차 | 발표 환경 카메라로 사전 리허설, `PHYS_*` 상수로 빠르게 재보정 가능하게 코드 구조화 |

---

## 8. 왜 이 프로젝트인가 (동료 평가 관점)

- **체험형**: 웹캠 하나만 있으면 되니 폰 페어링 없이 바로 서서 검+방패 들고 몸으로 싸우는 재미 → 캠프생들이 줄 서서 해보고 싶어지는 종류
- **기술 스토리 명확**: "웹캠 Pose 랜드마크만으로 7가지 동작을 실시간, 낮은 오탐률로 구분해내는 모션 인식 파이프라인"이라는 도전과제가 발표에서 바로 설명 가능
- **관객 참여 데모**: 발표 때 직접 시연/체험시키기 최적, 폰이 필요 없어 셋업이 빠름
- **공유 유도**: 클리어 랭크/기록을 캡처해서 공유하고 싶어지는 구조

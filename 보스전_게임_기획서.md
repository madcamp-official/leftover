# 프로젝트 기획: 듀얼 센서 모션 인식 보스전 게임 (가제)

## 1. 한 줄 컨셉

**노트북 웹캠과 스마트폰 2대(검+방패)의 가속도/자이로 센서를 결합해 내 몸동작을 그대로 게임 캐릭터에 매핑하고, 실제로 검을 휘두르고 방패로 막으며 보스와 검술 대결을 벌이는 3D 액션 게임.**

플레이어는 오른손에 스마트폰(검), 왼손에 스마트폰(방패)을 들고 웹캠 앞 고정된 위치에서 플레이. 발은 떼지 않고 상체 동작만으로 대결한다. 웹캠이 회피 동작(앉기/좌우 움직임)을, 두 폰이 각각 검과 방패의 궤적·타이밍을 인식해 세 데이터를 결합함.

---

## 2. 핵심 게임 플레이 — 1차 MVP

**플레이어 vs 보스(컴퓨터) 1대1 검술 대결.**

- 보스는 정해진 랜덤 패턴으로 공격 예고 → 플레이어가 타이밍 맞춰 대응
- 공격/방어는 각각 별도 게이지를 소모하는 자원 관리형 판정 시스템
- 데미지를 충분히 넣으면 클리어, 실패하면 데미지를 입음
- 최종 결과: 클리어 타임/판정 성공률 기반 랭크(S/A/B) 산정

### 동작 목록 (7종)

**공격 — 검(폰1), 공격 게이지 소모. 게이지 부족 시 공격 불가**

| 동작 | 인식 원리 | 특징 |
|---|---|---|
| 가로 베기 | 폰1 자이로 yaw축 회전 패턴 | 표준 데미지. **앉기**로 회피 가능 |
| 세로 베기 | 폰1 자이로 pitch축 회전 패턴 | 표준 데미지. **좌우 움직이기**로 회피 가능 |
| 찌르기 | 폰1 가속도계 전방축 직선 가속 (회전 없음) | 데미지는 베기의 절반. **회피 불가**, 방어/패링으로만 막을 수 있음 — 회피만 반복하는 플레이를 견제하는 카운터 픽 |

**방어 — 방패(폰2), 방어 게이지 소모. 게이지 부족 시 방어 불가**

| 동작 | 인식 원리 | 특징 |
|---|---|---|
| 기본 방어 | 폰2를 가슴 높이로 들고 정지 | 방어 게이지를 많이 소모하지만 어떤 공격이든 막아줌. 반격 기회는 없음 — 가장 안전한 선택 |
| 패링 | 폰2를 가슴 높이에 뒀다가 몸 바깥으로 스윙 (튕겨내기) | 방어 게이지를 기본 방어보다 적게 소모. 타이밍을 정확히 맞추면 상대가 몇 초간 행동불가 — 하이 리스크 하이 리턴. 타이밍을 놓치면 그대로 피격 |

**회피 — 웹캠 Pose 인식, 게이지 소모 없음**

| 동작 | 인식 원리 | 특징 |
|---|---|---|
| 앉기 | 몸 전체 높이 변화 | 가로 베기 회피. 찌르기엔 무효 |
| 좌우로 움직이기 | 상체 좌우 위치 변화 (발은 고정, 상체만 이동) | 세로 베기 회피. 찌르기엔 무효 |

→ 각 동작 특성에 맞는 센서를 골라 쓴 것이 이 프로젝트의 핵심 설계 근거.
타이밍이 예민한 방어/패링은 응답이 빠른 폰 IMU에, 판정이 관대해도 되는 회피는 웹캠 Pose에 배정.

### 밸런스 설계 원칙

세 가지 대응 수단(방어 / 패링 / 회피)이 "안전함 vs 자원 소모 vs 리스크&보상"으로 뚜렷하게 갈리도록 설계한다.

- **기본 방어**: 게이지 소모 큼, 공격 종류 안 가리고 다 막음, 반격 없음 → 가장 안전한 선택
- **패링**: 게이지 소모 적음, 타이밍 성공 시 상대 큰 경직(하이 리스크 하이 리턴), 실패 시 그대로 피격
- **회피 (앉기/좌우)**: 게이지 소모 없음(무료), 공격 종류를 정확히 읽어야만 성공, **성공 시 상대에게 짧은 경직(반격 찬스)을 부여** — 패링보다는 약하지만 무자원으로 반격 기회를 얻을 수 있음
- **찌르기**: 회피로는 절대 못 피함 → 회피만 쓰는 플레이를 견제

방어만 있으면 "게이지 있으면 무조건 방어"로 수렴해 수 싸움이 사라지기 때문에, 회피에 반격 보상을 붙여 세 옵션이 각자 다른 이유로 선택되게 만든 것이 이번 설계의 핵심 튜닝 포인트.

### 열린 질문 (구현하면서 플레이테스트로 확정할 것)

- [ ] 공격/방어 게이지 최대치, 초당 회복량
- [ ] 회피 성공 시 부여하는 경직 시간 (패링 경직보다는 짧게 — 구체적 수치는 실측)
- [ ] 판정 윈도우: 폰 IMU 기반(방어/패링/공격 분류)과 웹캠 기반(회피)이 서로 다름 → 3-3 참고

---

## 3. 기술적 도전 과제 (발표 핵심 포인트)

이번 프로젝트의 진짜 도전과제는 **"서로 다른 세 개의 센서 스트림(검 폰, 방패 폰, 웹캠)을 낮은 지연시간으로 동기화하고 통합 판정하는 것"**.

### 3-1. 시간 동기화 (Time Synchronization)

폰과 PC는 서로 다른 시계를 가지고 있어서, 폰에서 감지한 스윙이 "PC 기준 언제 발생했는지" 정확히 알아야 웹캠 데이터와 정렬 가능. **폰이 2대(검/방패)이므로 아래 프로토콜을 폰마다 독립적으로 수행**한다.

**해결법: NTP 방식 핑퐁 프로토콜**
```
1. PC가 폰에 timestamp(T1)와 함께 ping 전송
2. 폰이 수신 시각(T2) 기록 후, 전송 시각(T3)과 함께 pong 응답
3. PC가 pong 수신(T4)
4. RTT = (T4-T1) - (T3-T2)
5. Clock Offset = ((T2-T1)+(T3-T4)) / 2
```
연결 시 폰마다 20회 정도 반복해 offset 평균/중앙값으로 보정.

### 3-2. 네트워크 지연 최소화
- 폰↔PC는 **UDP** 통신 (TCP는 패킷 손실 시 재전송 대기로 지연 발생, 센서 스트리밍엔 UDP가 적합)
- 같은 로컬 네트워크로 인터넷 경유 없이 통신. **폰이 2대이므로 검 폰(폰1)이 핫스팟을 켜고 방패 폰(폰2)+PC가 거기 접속**하는 구성을 기본으로 한다 (7장 리스크 참고)
- 페이로드 최소화(가속도 3축 + 자이로 3축 + timestamp만 전송)

### 3-3. 체감 반응속도 확보
- 폰 신호(빠름, 100Hz+)는 즉시 이펙트 발동, 정확한 판정 확정은 살짝 뒤에 처리해도 체감상 "즉각 반응"
- 웹캠 기반 회피(앉기/좌우)는 판정 윈도우를 관대하게(±250~300ms) 설정
- 예측 보간(칼만 필터 or 선형 외삽)으로 실측 데이터 도착 전에 미리 캐릭터를 움직이고 나중에 보정

### 3-4. 모션 분류 알고리즘 (난이도별 선택지)

| 단계 | 방법 | 난이도 |
|---|---|---|
| Tier 1 | 가속도/각속도 임계값 기반 분류 | 낮음 (우선 구현) |
| Tier 2 | DTW(Dynamic Time Warping)로 궤적 유사도 비교 | 중 (시간 남으면 업그레이드) |
| Tier 3 | 경량 CNN/LSTM 직접 학습 | 높음 (야심작, 데이터 수집 부담) |

**추천: Tier 1로 먼저 완성 → 여유 있으면 Tier 2로 고도화**

검 폰(폰1)은 **가로/세로/찌르기 3분류**, 방패 폰(폰2)은 **방어(정지) / 패링(정지 후 바깥으로 스윙) 상태머신**까지 인식해야 해서 Tier 1이어도 임계값 튜닝이 예전(스윙 vs 찌르기 2분류)보다 까다롭다. 플레이어마다 스윙 습관이 달라 오분류가 체감 UX를 크게 해칠 수 있으므로, **접속 시 샘플 스윙 몇 회로 임계값을 보정하는 간이 캘리브레이션 모드**를 넣는 걸 권장한다.

---

## 4. 시스템 아키텍처

```
[폰1: 검] Unity 빌드 센서 스트리머
   → 가속도/자이로 raw data → UDP 전송 (60~100Hz), device="sword"

[폰2: 방패] Unity 빌드 센서 스트리머
   → 가속도/자이로 raw data → UDP 전송 (60~100Hz), device="shield"

[PC 웹캠] → Unity WebCamTexture → 자세 인식 (앉기 / 좌우 움직이기만 담당)

[Unity 메인 게임]
   → 폰1 / 폰2 / 웹캠, 세 스트림 수신 + 폰별 시간 동기화
   → 모션 분류 (검: 가로/세로/찌르기 3분류, 방패: 방어/패링 상태머신)
   → 판정 윈도우 매칭 → 게이지 소모/회복 + 데미지·경직 처리
   → 3D 캐릭터 애니메이션 매핑 + 보스 AI
```

**폰 클라이언트는 Unity로 직접 제작 추천**
- 브라우저 센서 API보다 권한/플랫폼 이슈 적음
- PC 쪽 게임 엔진과 언어(C#) 통일로 개발 속도 유리
- 폰1(검)·폰2(방패) 모두 동일한 센서 스트리머 앱을 쓰되 `device` 필드만 다르게 설정해서 재사용

**웹캠 비전 처리 방식 (택1)**
- Python(MediaPipe) 분리 프로세스 + UDP로 Unity 전송 → 빠른 프로토타이핑, 약간의 통신 오버헤드
- Unity Sentis 내장 처리 → 레이턴시 유리하지만 세팅 난이도 높음
- **추천: 처음엔 Python 분리 방식으로 시작, 레이턴시 문제 되면 Sentis 전환 검토**

통신 프로토콜(패킷 포맷, 포트, `device` 필드 등)의 상세 스펙은 [`shared/PROTOCOL.md`](shared/PROTOCOL.md) 참고.

---

## 5. 일정 (8일 기준)

| Day | 내용 |
|---|---|
| 1 | 폰(2대) 센서 스트리머 ↔ PC UDP 통신 검증, 폰별 시간 동기화 프로토콜 구현 |
| 2 | 웹캠 비전(앉기/좌우 움직이기) 연동, 임계값 기반 검 3분류·방패 상태머신 프로토타입 |
| 3 | 두 폰 + 웹캠 신호 통합 판정 시스템 구축, 캘리브레이션 모드 |
| 4 | 3D 캐릭터 애니메이션 매핑, 보스 상태머신 설계, 게이지 시스템 UI |
| 5 | 보스 패턴 2~3개 완성, 게이지/데미지/경직 시스템, 밸런스 1차 플레이테스트 |
| 6 | 레이턴시 튜닝 스프린트 (예측 보간, 판정 윈도우 조정) + 밸런스 튜닝(게이지 수치, 회피 경직 시간) |
| 7 | 이펙트/사운드/UI 폴리싱, 랭크/공유 시스템 |
| 8 | 버그픽스, 백업 데모 영상 녹화, 발표 리허설 |

---

## 6. 역할 분담 제안

- **A: 인식/판정 엔진** — 폰 2대 센서 통신, 폰별 시간 동기화, 검 3분류/방패 상태머신 모션 분류 로직, 게이지-판정 결합
- **B: 게임/비주얼** — 웹캠 비전 연동(회피 2종), 3D 캐릭터/보스, 게이지 UI, 이펙트

---

## 7. 리스크 및 대비책

| 리스크 | 대비책 |
|---|---|
| 발표장 와이파이 혼잡으로 UDP 지연/손실 | 검 폰(폰1) 핫스팟에 방패 폰(폰2)+PC 직결 (인터넷 미경유) |
| 폰 2대 동시 조작 중 그립 놓침/충돌 | 손목 스트랩 또는 케이스 고정, 리허설로 스윙 동작 반경 확인 |
| 폰 배터리/발열 (고주기 센서+전송 지속, 2대分) | 예비 폰, 보조배터리 준비 |
| 조명 환경에 따른 웹캠 인식률 저하 | 발표 전 현장 조명 리허설 필수 |
| 검 3분류(가로/세로/찌르기) 오분류로 엉뚱한 회피 유발 | 접속 시 샘플 스윙 캘리브레이션 모드 (3-4 참고) |
| 방어가 항상 안전해 회피가 무의미해지는 밸런스 붕괴 | 회피 성공 시 반격 찬스 부여로 트레이드오프 설계 (2장 참고) |
| 라이브 데모 중 인식 실패 | 백업 플레이 영상 사전 녹화 |
| 두 폰 + 웹캠 신호 동시 도착 시 처리 순서 버그 | 레이스 컨디션 케이스 별도 테스트 |

---

## 8. 왜 이 프로젝트인가 (동료 평가 관점)

- **체험형**: 직접 검+방패 들고 몸으로 싸우는 재미 → 캠프생들이 줄 서서 해보고 싶어지는 종류
- **기술 스토리 명확**: "세 개의 센서 스트림 융합 + 시간 동기화 + 레이턴시 최적화"라는 도전과제가 발표에서 바로 설명 가능
- **관객 참여 데모**: 발표 때 직접 시연/체험시키기 최적
- **공유 유도**: 클리어 랭크/기록을 캡처해서 공유하고 싶어지는 구조

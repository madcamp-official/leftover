# 프로젝트 기획: 듀얼 센서 모션 인식 보스전 게임 (가제)

## 1. 한 줄 컨셉

**노트북 웹캠과 스마트폰 가속도 센서를 결합해 내 몸동작을 그대로 게임 캐릭터에 매핑하고, 실제로 검을 휘두르고 방패로 막으며 보스와 싸우는 3D 액션 게임.**

플레이어는 한 손에 스마트폰(검), 다른 손은 맨손(방패)으로 플레이. 웹캠이 자세를, 폰이 손동작 궤적을 인식해 두 데이터를 결합함.

---

## 2. 핵심 게임 플레이

- 플레이어는 웹캠 앞에서 4가지 동작으로 전투
- 보스는 정해진 랜덤 패턴으로 공격 예고 → 플레이어가 타이밍 맞춰 대응
- 데미지를 충분히 넣으면 클리어, 실패하면 데미지를 입음
- 최종 결과: 클리어 타임/판정 성공률 기반 랭크(S/A/B) 산정

### 4가지 동작과 인식 방식

| 동작 | 담당 센서 | 인식 원리 |
|---|---|---|
| **방패 패링** | 웹캠 (자세 인식) | 손을 정면에 들어 방어 자세 판별 |
| **검 횡베기** | 폰 자이로 (yaw축) | 회전 각속도 패턴 |
| **검 찌르기** | 폰 가속도계 (전방축) | 회전 없이 직선 가속 패턴 |
| **상단공격 회피(쭈그리기)** | 웹캠 (Pose 인식) | 몸 전체 높이 변화 |

→ 각 동작 특성에 맞는 센서를 골라 쓴 것이 이 프로젝트의 핵심 설계 근거.
비전(자세/위치)엔 웹캠이, 궤적/속도가 중요한 동작엔 폰 IMU가 더 적합.

---

## 3. 기술적 도전 과제 (발표 핵심 포인트)

이번 프로젝트의 진짜 도전과제는 **"서로 다른 두 디바이스에서 오는 센서 데이터를 낮은 지연시간으로 동기화하고 통합 판정하는 것"**.

### 3-1. 시간 동기화 (Time Synchronization)

폰과 PC는 서로 다른 시계를 가지고 있어서, 폰에서 감지한 스윙이 "PC 기준 언제 발생했는지" 정확히 알아야 웹캠 데이터와 정렬 가능.

**해결법: NTP 방식 핑퐁 프로토콜**
```
1. PC가 폰에 timestamp(T1)와 함께 ping 전송
2. 폰이 수신 시각(T2) 기록 후, 전송 시각(T3)과 함께 pong 응답
3. PC가 pong 수신(T4)
4. RTT = (T4-T1) - (T3-T2)
5. Clock Offset = ((T2-T1)+(T3-T4)) / 2
```
연결 시 20회 정도 반복해 offset 평균/중앙값으로 보정.

### 3-2. 네트워크 지연 최소화
- 폰↔PC는 **UDP** 통신 (TCP는 패킷 손실 시 재전송 대기로 지연 발생, 센서 스트리밍엔 UDP가 적합)
- 같은 로컬 네트워크(가능하면 폰 핫스팟으로 PC 직결)로 인터넷 경유 없이 통신
- 페이로드 최소화(가속도 3축 + 자이로 3축 + timestamp만 전송)

### 3-3. 체감 반응속도 확보
- 폰 신호(빠름, 100Hz+)는 즉시 이펙트 발동, 정확한 판정 확정은 살짝 뒤에 처리해도 체감상 "즉각 반응"
- 웹캠 기반 동작은 판정 윈도우를 관대하게(±250~300ms) 설정
- 예측 보간(칼만 필터 or 선형 외삽)으로 실측 데이터 도착 전에 미리 캐릭터를 움직이고 나중에 보정

### 3-4. 모션 분류 알고리즘 (난이도별 선택지)

| 단계 | 방법 | 난이도 |
|---|---|---|
| Tier 1 | 가속도/각속도 임계값 기반 분류 | 낮음 (우선 구현) |
| Tier 2 | DTW(Dynamic Time Warping)로 궤적 유사도 비교 | 중 (시간 남으면 업그레이드) |
| Tier 3 | 경량 CNN/LSTM 직접 학습 | 높음 (야심작, 데이터 수집 부담) |

**추천: Tier 1로 먼저 완성 → 여유 있으면 Tier 2로 고도화**

---

## 4. 시스템 아키텍처

```
[폰] Unity 빌드 센서 스트리머
   → 가속도/자이로 raw data → UDP 전송 (60~100Hz)

[PC 웹캠] → Unity WebCamTexture → 자세/손 인식

[Unity 메인 게임]
   → 두 스트림 수신 + 시간 동기화
   → 모션 분류 (스윙 종류 판별)
   → 판정 윈도우 매칭 → 데미지 처리
   → 3D 캐릭터 애니메이션 매핑 + 보스 AI
```

**폰 클라이언트는 Unity로 직접 제작 추천**
- 브라우저 센서 API보다 권한/플랫폼 이슈 적음
- PC 쪽 게임 엔진과 언어(C#) 통일로 개발 속도 유리

**웹캠 비전 처리 방식 (택1)**
- Python(MediaPipe) 분리 프로세스 + UDP로 Unity 전송 → 빠른 프로토타이핑, 약간의 통신 오버헤드
- Unity Sentis 내장 처리 → 레이턴시 유리하지만 세팅 난이도 높음
- **추천: 처음엔 Python 분리 방식으로 시작, 레이턴시 문제 되면 Sentis 전환 검토**

---

## 5. 일정 (8일 기준)

| Day | 내용 |
|---|---|
| 1 | 폰 센서 스트리머 ↔ PC UDP 통신 검증, 시간 동기화 프로토콜 구현 |
| 2 | 웹캠 비전(패링/쭈그리기) 연동, 임계값 기반 스윙/찌르기 분류 |
| 3 | 두 센서 신호 통합 판정 시스템 구축 |
| 4 | 3D 캐릭터 애니메이션 매핑, 보스 상태머신 설계 |
| 5 | 보스 패턴 2~3개 완성, 데미지/체력 시스템 |
| 6 | 레이턴시 튜닝 스프린트 (예측 보간, 판정 윈도우 조정) |
| 7 | 이펙트/사운드/UI 폴리싱, 랭크/공유 시스템 |
| 8 | 버그픽스, 백업 데모 영상 녹화, 발표 리허설 |

---

## 6. 역할 분담 제안

- **A: 인식/판정 엔진** — 폰 센서 통신, 시간 동기화, 모션 분류 로직
- **B: 게임/비주얼** — 웹캠 비전 연동, 3D 캐릭터/보스, 이펙트, UI

---

## 7. 리스크 및 대비책

| 리스크 | 대비책 |
|---|---|
| 발표장 와이파이 혼잡으로 UDP 지연/손실 | 폰 핫스팟으로 PC 직결 (인터넷 미경유) |
| 폰 배터리/발열 (고주기 센서+전송 지속) | 예비 폰, 보조배터리 준비 |
| 조명 환경에 따른 웹캠 인식률 저하 | 발표 전 현장 조명 리허설 필수 |
| 라이브 데모 중 인식 실패 | 백업 플레이 영상 사전 녹화 |
| 두 센서 신호 동시 도착 시 처리 순서 버그 | 레이스 컨디션 케이스 별도 테스트 |

---

## 8. 왜 이 프로젝트인가 (동료 평가 관점)

- **체험형**: 직접 검+방패 들고 몸으로 싸우는 재미 → 캠프생들이 줄 서서 해보고 싶어지는 종류
- **기술 스토리 명확**: "두 디바이스 센서 융합 + 시간 동기화 + 레이턴시 최적화"라는 도전과제가 발표에서 바로 설명 가능
- **관객 참여 데모**: 발표 때 직접 시연/체험시키기 최적
- **공유 유도**: 클리어 랭크/기록을 캡처해서 공유하고 싶어지는 구조
